spring:
  application:
    name: member-service
  datasource:
    url: jdbc:mysql://localhost:3309/fitmate_local
    username: fitmate_local
    password: fitmate12!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
server:
  port: 8000
  servlet:
    context-path: /member-service
